# Learn Languages App - Production Dependencies (APK / runtime)
# Mismas libs y versiones que requirements.txt (dev). Se excluyen SOLO:
#   - herramientas de dev: ruff, mypy, pytest, pytest-asyncio
#   - pycaw: control de volumen de Windows (Core Audio); no aplica en Android
#           (Volumer hace no-op fuera de Windows)
#
# UNICA divergencia permitida con el dev: el limite INFERIOR de las libs con parte
# nativa (pydantic-core, jiter), porque en Android solo existe el wheel que haya
# horneado pypi.flet.dev y ese indice va por detras de PyPI. Aqui se deja la banda
# ~=X.Y.Z mas baja que cubra el wheel movil disponible; quien resuelve de verdad las
# deps del APK es pyproject.toml [project].dependencies, no este fichero.

# CORE
python-dotenv~=1.2.3

# UI FRAMEWORK
flet==1.0.0
flet-audio==1.0.0

# DATABASE
aiosqlite~=0.22.1

# VALIDATION
# ~=2.13.4 (no 2.13.5) a proposito: la 2.13.5 pide pydantic-core==2.46.5 y en
# pypi.flet.dev el wheel Android mas nuevo es 2.46.4 -> en el APK se monta 2.13.4.
pydantic~=2.13.4

# AI/ML
# TOPADO a 2.x (ultima: 2.54.0): la 3.x exige jiter>=0.16 y el wheel Android mas
# nuevo es 0.15.0. La 2.54.0 pide jiter>=0.10 y por eso si entra en el APK.
# El porque completo, en requirements.txt.
openai~=2.54.0

# UTILITIES
pyperclip~=1.11.0
