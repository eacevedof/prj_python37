import os

paththisdir=os.path.dirname(os.path.abspath(__file__))
PATH_ROOT=os.path.abspath(f"{paththisdir}/../../")

PATH_CONFIG=f"{PATH_ROOT}/config"
PATH_LOGS=f"{PATH_ROOT}/logs"
PATH_PUBLIC=f"{PATH_ROOT}/public"
PATH_SRC=f"{PATH_ROOT}/src"
PATH_TESTS=f"{PATH_ROOT}/tests"
